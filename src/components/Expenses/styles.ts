import styled from "styled-components";

const StyledExpenses = styled.div``;

const Title = styled.h2`
  font-weight: 700;
  font-size: 24px;
  line-height: 29px;
`;

export { StyledExpenses, Title };
