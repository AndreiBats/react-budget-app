import React from "react";
import { SubTitle } from "../Remaining/styles";

import { StyledRemaining } from "./styles";

export const Remaining = () => {
  return (
    <StyledRemaining>
      <SubTitle>Remaining: 2000$</SubTitle>
    </StyledRemaining>
  );
};
