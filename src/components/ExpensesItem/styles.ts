import styled from "styled-components";

const StyledExpensesItem = styled.div`
  display: flex;
`;

const Name = styled.span``;

const Close = styled.span``;

export { Name, StyledExpensesItem, Close };
