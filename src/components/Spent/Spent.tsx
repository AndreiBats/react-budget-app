import React from "react";
import { SubTitle } from "../Spent/styles";
import { StyledSpent } from "./styles";

export const Spent = () => {
  return (
    <StyledSpent>
      <SubTitle>Spent so far: $1000</SubTitle>
    </StyledSpent>
  );
};
