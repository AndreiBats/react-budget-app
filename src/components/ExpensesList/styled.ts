import styled from "styled-components";

const StyledExpensesList = styled.ul`
  list-style: none;
  margin: 30px 0;
  overflow-y: auto;
  max-height: 213px;
`;

export { StyledExpensesList };
